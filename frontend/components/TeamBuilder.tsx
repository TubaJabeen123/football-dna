// TeamBuilder.tsx
// This component is reserved for future use.
// The team-building logic currently lives inside worldcup/page.tsx.
// Export a no-op default to prevent import errors if referenced.
export default function TeamBuilder() {
  return null;
}
